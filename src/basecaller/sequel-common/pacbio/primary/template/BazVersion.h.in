#ifndef BAZIO_VERSION

#define BAZ_MAJOR_VERSION ${Baz_MAJOR_VERSION}
#define BAZ_MINOR_VERSION ${Baz_MINOR_VERSION}
#define BAZ_PATCH_VERSION ${Baz_PATCH_VERSION}
#define BAZ_VERSION "${Baz_MAJOR_VERSION}.${Baz_MINOR_VERSION}.${Baz_PATCH_VERSION}"

#define BAZ_BASECALLER_ALGO_VERSION "${Baz_BASECALLER_ALGO_VERSION}"

#define BAZIO_VERSION "${BazIO_VERSION}"
#define BAZIO_P4VERSION "${P4VERSION}"

#endif